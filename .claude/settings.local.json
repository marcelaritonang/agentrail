{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git -c user.name=\"ApplyMate\" -c user.email=\"dev@applymate.local\" commit -q -m \"Checkpoint: working FastAPI + vanilla-JS app before Next.js migration\")"
    ]
  }
}

{
  "name": "@agentrail-sdk/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/runtime.js"
  },
  "dependencies": {
    "@agentrail-sdk/blob": "workspace:*",
    "@agentrail-sdk/config": "workspace:*",
    "@agentrail-sdk/contracts": "workspace:*",
    "@agentrail-sdk/db": "workspace:*",
    "@agentrail-sdk/pricing": "workspace:*",
    "@agentrail-sdk/queue": "workspace:*",
    "@aws-sdk/client-s3": "^3.1091.0"
  }
}
